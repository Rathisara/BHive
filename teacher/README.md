# Teacher App

A minimal React + Vite starter for the `teacher` workspace.

Commands:

```bash
npm install
npm run dev    # starts development server at http://localhost:5173
npm run build  # build for production
npm run preview
```
